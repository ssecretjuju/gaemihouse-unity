using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;
using UnityEngine.Networking;


//상호작용할 기능: 글쓰기 게시판, 주식 예측 게시판, 화면공유 게시판, 의자에 앉기
//상호작용한 오브젝트 앞에서 F표시가 뜨게한다.
//글쓰기 게시판 앞에서 F를 누르면 BoardCanvas가 활성화된다.

public class _InteractableManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
