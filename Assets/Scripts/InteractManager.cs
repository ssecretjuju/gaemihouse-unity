using System.Collections;
using System.Collections.Generic;
using UnityEngine;


//상호작용한 오브젝트 앞에서 F표시가 뜨게한다.
//글쓰기 게시판 앞에서 F를 누르면 BoardCanvas가 활성화된다.

public class IInteractableManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
